# Client (Expo SDK 54)
- Remember to update SERVER constant in App.js to point to your machine IP if testing on a real device.
- Run: npm install && npm start
- If you face peer deps issues, run: npm install --legacy-peer-deps